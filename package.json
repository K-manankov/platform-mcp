{
  "name": "@sonar/platform-mcp",
  "version": "0.1.0",
  "description": "MCP-сервер для инфраструктурных сервисов (Argo CD, Vault) с входом через GitLab SSO",
  "type": "module",
  "bin": {
    "platform-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && find dist -name '*.test.js' -print0 | xargs -0 node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
